import React, { FC, ReactElement } from 'react';

const NotFound404: FC = (): ReactElement => {
    return (
        <div>
            404 not found
        </div>
    );
};

export default NotFound404;